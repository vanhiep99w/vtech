<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="007-create-brands-table" author="dongdang">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="brands"/>
            </not>
        </preConditions>

        <createTable tableName="brands" remarks="Bảng brands">
            <column name="id" type="VARCHAR(36)">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="brand_name" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>

            <column name="slug" type="VARCHAR(50)">
                <constraints nullable="false"
                             unique="true"
                             uniqueConstraintName="uk_brand_slug"/>
            </column>

            <column name="brand_desc" type="VARCHAR(255)"/>

            <column name="brand_logo" type="VARCHAR(255)"/>

            <column name="display_order" type="INT"/>

            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="status" type="INT" defaultValue="1">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <!-- Indexes -->
        <createIndex tableName="brands" indexName="idx_brand_status">
            <column name="status"/>
        </createIndex>

        <createIndex tableName="brands" indexName="idx_brand_display_order">
            <column name="display_order"/>
        </createIndex>

        <createIndex tableName="brands" indexName="idx_brand_created">
            <column name="created_at"/>
        </createIndex>

        <rollback>
            <dropTable tableName="brands"/>
        </rollback>
    </changeSet>

</databaseChangeLog>